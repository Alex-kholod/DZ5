#ifndef DZ5_H
#define DZ5_H

#include <iostream>;
#include <ctime>;
using namespace std;

void recieve(double*, unsigned);
void recieve(int* arr, unsigned size);
void print(double*, unsigned);
void print(int* arr, unsigned size);
void replace(int*, unsigned);
void recieve(int**, int, int);
void print(int**, int, int);

#endif
